# Production Completion Audit

Date: 2026-08-24

Objective: make Insert Player production-ready: playable from different devices with quality tiers and associated pricing, Clerk user management, server-side API proxy, rate limiting, and cloud fighter storage/sync using the approved Cloudflare Worker/R2/D1 architecture. AI Street Fighter is the internal repository name only.

## Success Criteria

1. The game can be generated, synced, opened, and played from different devices for the same Clerk user.
2. Rookie, Contender, and Champion tiers are enforced end-to-end, priced, charged, and preserved as separate generated versions.
3. Clerk is the production identity layer, with Worker-side JWT verification and user persistence.
4. Provider API keys are server-side only in production behind a Cloudflare Worker proxy.
5. Expensive routes are rate limited by Clerk user id, with anonymous IP fallback.
6. Fighters, source views, sprite sheets, sprite versions, public community clones, billing rows, and match/stat data persist through D1/R2.
7. Community publishing is safe enough for launch: public playable assets are shareable without exposing original uploads or raw intermediates.
8. Stripe credit purchase and generation charge reserve/release/first-provider-commit flows are safe and idempotent; provider failures never restore committed credits.
9. Production deployment and live smoke tests have passed against real Cloudflare, Clerk, Stripe, and provider credentials.
10. Public launch branding is cleared and no production-facing surface uses the internal `AI Street Fighter` / `Street Fighter` name.

## Prompt-To-Artifact Checklist

| Requirement / named artifact | Evidence inspected | Status |
| --- | --- | --- |
| Read/keep orientation docs aligned: `CLAUDE.md`, `ROADMAP.md`, `QUALITY_TIERS.md` | Docs describe React-owned UI, no new Phaser UI scenes, Tailwind-only styling, tier decisions, Worker/R2/D1/Clerk/Stripe state. | Implemented locally |
| Public launch brand clearance | `BRANDING.md`, ignored `.brand-clearance.json`, and `scripts/check-launch.mjs` treat `AI Street Fighter` as internal-only. The selected public brand is `Insert Player` / `P1` at `https://insertplayer.ai`; the owner-directed record dated 2026-08-19 contains concrete USPTO/TMview/domain/public-surface evidence and accepts the documented moderate residual risk without implying outside-counsel review. Static metadata/assets and dynamic React/Worker share surfaces use the public brand. | Cleared for launch by recorded owner risk acceptance; not a legal opinion or registrability guarantee |
| Tier system: Rookie / Contender / Champion | `src/services/QualityTiers.ts`, `worker/src/tiers.ts`, `src/services/CharacterPipeline.ts`, `src/services/FrameSequence.ts`, and `QUALITY_TIERS.md` define tier ids, credit costs, GA Gemini model/pipeline differences, Rookie no BiRefNet anims, source views separate, and four-unique-to-seven-frame mirrored attacks; `scripts/check-tier-parity.mjs` structurally verifies frontend/Worker/pipeline parity, Pro source-view env docs, tier-controlled animation models, and bounded introductory price coverage; `scripts/check-production.mjs` runs that guard. | Implemented and tested locally |
| Source views always Pro | `CharacterPipeline.ts` scopes tier overrides to animation generation; source-view model envs remain separate in `GeminiApi.ts` and docs; `scripts/check-production.mjs` guards that source-view env examples stay Pro and animation tier overrides stay sprite-scoped. Durable source Retry accepts only `side`, `upright`, or `crouch` and retains Pro models server-side. | Implemented; real Pro Upright Workflow Retry passed in isolated QA |
| Official Arcade roster provenance and provider isolation | `arcade/roster-2026.json` pins 13 real reusable source photos by URL, attribution/license metadata, dimensions, and SHA-256. `scripts/fetch-arcade-sources.mjs` restores only the environment-specific private content-addressed R2 object and rejects byte, type, size, or hash drift. `scripts/seed-arcade-roster.mjs` requires explicit seed/resume/restart intent, preserves existing drafts and every version, and never activates. `.github/workflows/seed-arcade-production.yml` requires exact billable confirmation and fails before generation when the approved-provider assertion is absent or cannot prove Gemini-only runtime. The authenticated Worker preflight reads the deployed processor `/health` contract and accepts only Gemini Pro source views, a Gemini Flash pose scaffold, Gemini Pro visible Champion frames/review, and fail-closed fallback policy before the seeder lists or mutates Arcade inventory. | All 13 sources round-trip verified in private production R2; eight private drafts exist, five remain unseeded, zero are public, and Trump is the first post-reset QA gate |
| Account-isolated cache and requests preserve generated versions | `src/services/SpriteCache.ts` v5 scopes all IndexedDB stores by Clerk user and keys sprites by `[ownerScope, versionId]`; v4 data migrates to local ownership and is claimed by the first signed-in account. Stale writes after an account switch are rejected. `src/services/ApiClient.ts` captures immutable auth/provider-session request contexts, revalidates them around async token/fetch boundaries, and rejects account switches instead of attaching a new user's token to an old operation. Owned cloud fighter detail responses include active plus archived source/processed/RAW content hashes. A playable-first phase hydrates one best sprite per animation, then a resumable bounded queue fills every archived and RAW asset while persisting the completed remote-version count. Worker `sprite_versions` archives distinct sprite uploads and `source_versions` archives source-view uploads. | Implemented, behavior-tested, and authenticated across separate browser/origin stores; physical-device validation pending |
| Cloud sprite sync retry idempotency | `worker/migrations/0006_sprite_content_hash.sql` adds sprite/raw content hashes and a partial unique content index; `uploadFighterSprite` hashes uploaded bytes, makes version/current/tier writes one D1 transaction, chooses current from the archived canonical row, and removes only a staged R2 copy that lost a concurrent identical insert. Private manifests let `CloudFighters.ts` skip every known blob, upload missing history only, and call `PATCH /api/fighters/:id/sprites` to promote a known archived hash without bytes. Real Miniflare D1/R2 tests exercise two simultaneous identical processed/raw uploads; real QA promoted an alternate Champion WALK with no upload-rate row and retained its version count. | Implemented, binding-tested, deployed, and authenticated-sandbox validated |
| Cloud source sync retry idempotency | `worker/migrations/0007_source_versions.sql` adds source upload version rows and a partial unique content index; `uploadFighterSource` hashes uploaded bytes, transactionally chooses current from the archived canonical row, and removes only a staged R2 copy that lost a concurrent identical insert. Real Miniflare D1/R2 tests exercise two simultaneous identical uploads. | Implemented, binding-tested, and deployed |
| Upgrade regenerates from scratch and preserves old versions | Durable `fighter_upgrade` Workflow jobs regenerate animations at the target tier, archive unique clean/RAW R2 content, preserve all prior source/sprite rows, release only untouched reservations, and commit at the first billable provider attempt. | Implemented; real Contender and Champion upgrades passed in isolated QA |
| Clerk user management and deletion | `src/main.tsx` ClerkProvider/auth UI; `src/services/ApiClient.ts` attaches Clerk bearer tokens only to the API origin; `worker/src/auth.ts` validates issuer/JWKS/`azp` and normalizes profiles; `worker/src/clerkWebhooks.ts` verifies Clerk Standard Webhooks, de-duplicates events, syncs profile changes, drains account R2 prefixes in bounded retryable batches, deletes dependent D1 rows, and writes a hashed tombstone so pre-deletion JWTs cannot recreate the account. IndexedDB v5 separately isolates local data between browser users and rejects stale writes after account switches. | Development lifecycle and production Action `32743113442` passed with two users; add the Development smoke key and rotate the exposed Production key after its replacement is verified |
| Durable generation across disconnects and partial failure | `worker/src/generationJobs.ts`, `generationWorkflow.ts`, `imageProcessorContainer.ts`, `generatedAssets.ts`, `generationAuth.ts`, migrations `0018`/`0019`/`0023`, and `src/services/GenerationJobs.ts` make authenticated creation, upgrade, animation Retry, and source Retry server-owned. Migration `0023` adds immutable source/sprite checkpoints, resumable run lineage, and response-backed provider idempotency. Jobs expose D1-backed progress, extend the reservation/session for Workflow execution, release only pre-provider reservations, and retain committed credits after provider failure. Identical concurrent starts replay one job; a different competing authorization releases its untouched reservation. Create/Gallery reconnect after navigation or network loss and can resume only the pending stages. | Production job `f3b6c650cbb2e1bf796eebb79edff5c7` is backfilled as partial at `sprite:low_punch` with seven approved clean/RAW checkpoints; regression proves resume skips its three sources plus four completed animations |
| Server-side provider proxy | `worker/src/proxy.ts` routes Gemini/FAL/Runway/Freepik/Ludo/upload-temp/image/media; frontend services call through `src/services/ApiClient.ts`. Provider proxy routes are allowlisted to the app's known endpoints instead of acting as generic tunnels, broad Ludo result listing is blocked, and `/proxy/upload-temp` rejects requests without a session before body parsing/R2 writes, then caps multipart bytes while streaming before parsing. Provider POST bodies are also capped while streaming (48 MB Gemini, 24 MB other providers). `/proxy/image`/`/proxy/media` require active provider sessions before fetching public result URLs, handle redirects manually with every hop revalidated, require HTTPS and supported MIME, block private/special IPv4 and all IPv6 literals, cap response bodies while streaming, and apply bounded provider/result timeouts. Result/temp asset responses include `nosniff`. Provider jobs and their status/result GETs require a D1-backed `provider_sessions` row and `X-ASF-Provider-Session`, minted by generation authorization or authenticated feature sessions, with purpose/tier route validation, GA Gemini model allowlisting, atomic call limits, and conservative per-session cost ceilings. Every attempted upstream write increments an atomic monthly aggregate and permanent operation/model/outcome event, but aggregate spend never acts as a global kill switch. Only billable job calls consume session call/cost budget, so polling remains available after the final authorized job is launched; generation authorization responses expose `providerCallLimit` for direct smoke diagnostics; Worker CORS allows the provider-session header for browser preflight. | Worker `0.18.0` is deployed in QA and production; runtime source, compiled bundle, Container build, and Arcade preflight fail closed on unapproved BFL/FLUX/Klein integration |
| R2 upload abuse controls | `worker/src/fighters.ts` enforces source/sprite file size caps, PNG/JPEG/WebP byte-signature validation, declared-vs-detected MIME agreement, and bounded sprite frame metadata before R2 writes; `worker/src/proxy.ts` caps temp provider uploads before base64 decode/storage. `scripts/check-production.mjs` guards these controls. | Deployed and public-smoked |
| Browser debug logging / style hygiene | `src/services/DebugLog.ts` gates browser console output to dev mode or explicit `asf:debug` / `window.__ASF_DEBUG_LOGS__`; app services/routes/scenes use `debugInfo`/`debugWarn`; `scripts/check-production.mjs` rejects raw `console.log/info/warn` in production app source outside the debug helper. `scripts/check-no-inline-styles.mjs` now scans `src/ui` and `src/game`, so React UI and match-scene DOM overlays cannot reintroduce inline styles. | Implemented locally |
| No production client API keys | `.env.example` / `.env.production.example` contain frontend-only vars; `vite.config.ts` local proxy reads non-`VITE_` secret names only and redacts secret-bearing URLs/status logs; `worker/wrangler.toml` and `PRODUCTION_READINESS.md` put provider keys in Worker secrets; `scripts/check-production.mjs` rejects client-exposed `VITE_*_API_KEY` provider envs in source/config/env examples and guards Vite dev-proxy redaction. | Provider secrets are installed in both environments; Stripe test secrets remain sandbox-only and Live Stripe secrets are staged only in GitHub production |
| Anonymous abuse and rate limits | `worker/src/turnstile.ts` validates a single-use managed Turnstile token, action, hostname, and connecting IP before anonymous Rookie can mint a provider session; production fails closed on missing configuration or Siteverify failure. `worker/src/rateLimit.ts`, `worker/src/auth.ts`, `worker/src/proxy.ts`, `worker/src/providerSessions.ts`, and `worker/src/index.ts` additionally enforce Clerk-user/IP route limits with an atomic D1 UPSERT, including one anonymous Rookie authorization per pseudonymized network identity/day. Counters cannot over-admit through a read/write race, report the remaining fixed-window `Retry-After`, and prune expired rows. Unit/structural checks cover success, missing token, config failure, wrong action/hostname, provider-session ordering, and counter behavior. | Deployed and live-validated on 2026-08-24: one real token returned `200` with one session; exact replay returned `403 turnstile_failed` without a second session or provider call |
| Cloud fighter storage/sync across devices | `worker/src/fighters.ts`, migrations `0002`/`0004`, `src/services/CloudFighters.ts`, Gallery/Roster refresh missing and newer owned cloud fighters, preserve cloud fighter ids for match reporting, fetch private content-hash manifests, use the Worker/D1 `updatedAt` timestamp for imported metadata freshness, paint the highest playable tier first, hydrate all lower-tier/history/RAW assets in a resumable serial queue, promote known versions without re-uploading bytes, and continue importing the rest of the roster if one cloud fighter is bad. Archive-only uploads advance the fighter timestamp so already-synced devices discover new history. `src/game/sprites/AiSpriteLoader.ts` fallback-fills missing runtime states from mapped fallbacks, idle, or the first available imported animation so partial private cloud imports do not render transparent move rows. Gallery rename/delete patches/deletes the cloud fighter when a synced `cloudFighterId` exists. `scripts/check-production.mjs` guards this wiring. | Implemented and deployed; separate-browser/origin QA loaded a fresh 48-version fighter in 4.776s and completed background history; physical-device validation remains |
| R2/D1 approved architecture | `worker/wrangler.toml`, `worker/src/fighters.ts`, migrations `0001`-`0024`, `PRODUCTION_READINESS.md`. Clean EU resources are D1 `insert-player-db` (`aa9e6ba6-8a7f-4261-ac31-986e6cf44659`) and R2 `insert-player-assets`; automatic read replication, Time Travel, version archives, per-session usage, atomic unbounded monthly provider accounting, permanent per-call cost events, durable jobs, immutable artifact checkpoints, resumable asset deletion, upload indexes, and the temp lifecycle are defined. The legacy rolling-reservation table is retained only for migration compatibility and scheduled cleanup, not request admission. R2 has no public development URL, direct custom domain, or bucket CORS policy. | Production is isolated through `0024`; Worker `0.18.0` and remote postdeploy audit pass |
| Isolated Cloudflare QA stack | `worker/wrangler.sandbox.toml`, `.env.sandbox.example`, sandbox-only package scripts, `scripts/smoke-sandbox.mjs`, and production structural guards pin QA to Worker `insert-player-api-sandbox`, Pages `insert-player-sandbox`, EU D1 `insert-player-sandbox-db` (`f60b6e22-d262-4e46-a7d9-ca095e49d102`), EU R2 `insert-player-sandbox-assets`, its own Workflow, and its own Container. Migrations `0001`-`0024`, one-day temp lifecycle, Clerk/Stripe/provider/anonymization/job-signing secrets, isolated durable spend accounting, production-origin CORS denial, explicit anonymous-Rookie shutdown, and absence of live Stripe are verified. Production and sandbox resources, Stripe key modes, env files, webhook origins, CSP allowlists, and spend ledgers cannot overlap. | Develop `1d3ea1e` is live as Worker `0.18.0`; deployment Action `32767773857` and authenticated Clerk/D1/R2 Action `32768251105` pass |
| Associated pricing and credits | `worker/src/billing.ts`, `src/services/Billing.ts`, HomePage, Create/Gallery tier UI, Stripe tables, and `scripts/bootstrap-stripe-catalog.mjs`. Packs are 11 credits / €14.99, 20 / €24.99, and 47 / €56.99. Full generation/upgrade costs 2/11/18; animation retries 1/2/4; Pro source retries 1; RAW rebuilds are free. `check:tiers` computes net revenue after VAT, Stripe EEA-card fees and Stripe Tax, enforces at least 1.30× clean measured-cost coverage, 1.25× retry coverage, and 1.10× coverage for the real `$32.64` failure-heavy QA sequence. Migration `0017` adds durable per-operation provider cost events without allowing maintenance to delete them. Bootstrap isolates environments, denies ShellBot, deactivates stale prices, and preserves billing history. | Profitable v2 catalog bootstrapped; two authenticated €14.99 Starter smokes each granted 11 credits once (wallet 6→17→28) while preserving the historical 6-credit purchase |
| Mobile/local play input | `src/game/systems/VirtualInput.ts`, `src/game/systems/InputManager.ts`, `src/ui/components/MobileFightControls.tsx`, `GamePage`, and Tailwind component CSS provide multitouch coarse-pointer controls plus browser gamepad input without moving UI ownership into Phaser. Vitest covers held movement, one-shot attacks, simultaneous input, player isolation, and reset. Mobile-browser emulation at `390x844` kept all eight controls in view and real touch events moved and attacked. A full Attract Mode match completed two rounds and the accessible React result actions replaced keyboard-only instructions; Run It Back restarted the same matchup cleanly. A route-wide mobile DOM audit found no horizontal overflow, duplicate ids, missing image alt attributes, or unnamed standalone controls; independent legal/footer/consent links now retain 44px tap targets. | Implemented, unit-tested, and browser-tested locally; real-phone portrait/landscape QA pending |
| Dependency security | `npm audit --omit=dev`, Clerk 6.14.x, patched protobufjs, Vite 7.3.6, and refreshed build transitive dependencies. | Zero known production or development vulnerabilities on 2026-08-17 |
| Generation reservation/release/commit | `worker/src/billing.ts` verifies fighter ownership before reserving free quota/credits. `providerSessions.ts` transactionally commits that charge with the first billable provider cost event. Only a reservation that never reaches external AI can be released; unsuccessful provider responses retain charge, session usage, monthly spend, and a failed cost event. A same-request start race replays the existing job; a genuinely competing authorization releases its untouched reservation. The legacy client completion path obeys the same status boundary. | Unit and real-D1 tests cover pre-provider release, atomic first-provider commit, upstream `429` retention, duplicate release, concurrent release cleanup, and job races; sandbox deploy/smoke passes and live remains |
| Stripe checkout idempotency and account isolation | `worker/src/billing.ts` rejects missing current legal attestation and unconfigured checkout before writing D1 rows, blocks test-mode Stripe events/checkout on the production Worker, verifies `/v1/account` against `STRIPE_ACCOUNT_ID`, verifies active inclusive-tax EUR Price/Product entries with video-game tax code `txcd_10201000`, creates one reusable Stripe Customer per Clerk user with stable idempotency, and pre-reserves a local checkout row before Stripe is called. Checkout enables automatic tax, reuses the Customer address, and disables Stripe's duplicate generic Terms checkbox; Insert Player's stronger versioned legal attestation is persisted before redirect. Webhooks credit once, reconcile refunds/disputes idempotently, tolerate out-of-order events, and allow negative balances when already-spent value is reversed. Bootstrap preserves the wildcard endpoint, refuses denylisted accounts, preserves history, labels catalog metadata by environment, rejects cross-mode keys/webhook origins, and requires `--allow-live` for live mutations. | Sandbox v2 11-credit Starter payment/webhook passed with one `+11` ledger row and preserved historical `+6`; Live profile, catalog, three prices, wildcard webhook, variables, and secrets are configured; one real Starter purchase remains |
| Operational data minimization and retention | `worker/src/maintenance.ts` releases only untouched reservations on jobs stalled for four days; committed stalled jobs remain consumed and are routed to repair/support. It deletes private provider-response replay blobs one day after terminal completion, removes terminal job telemetry after seven days, and only then removes unreferenced expired provider sessions. Existing schedules prune rate limits, minimized webhook markers, abandoned checkouts, closed moderation records, and legal evidence. Tests/guards reject deletion of users, tombstones, credit history, fighters, or generated source/sprite/RAW versions. | Retention implementation and guards pass locally; daily trigger is active |
| Photo consent and publication boundary | `GenerationConsent`, `/privacy`, `/terms`, Worker legal version `2026-08-23.1`, and production structural guards limit authorization to processing needed to create and privately save the fighter, disclose first-provider credit consumption/no automatic restoration, preserve mandatory remedies, and reject any general publication/reuse licence. Clerk is identity only and Stripe is billing only; neither receives the original upload. The original is never published. Insert Player does not sell the photo/fighter, use either in advertising, or use either to train models. Publish requires a separate confirmation and exposes only the chosen fighter's clean generated sources/playable assets under the neutral label `Player`; account names, emails, Clerk profile photos, Clerk/internal account ids, originals, RAW intermediates, private hashes, and version history remain private. | QA publication checks pass; deployed legal `2026-08-23.1` Cancellations and unchecked generation-consent copy were verified in QA and production |
| Community viral loop | `worker/src/fighters.ts`, `worker/src/index.ts`, `src/ui/routes/CommunityPage.tsx`, `src/ui/shared/communityShare.ts`, Gallery publish/share actions with a separate privacy confirmation, native mobile share sheet fallback to clipboard, public `/api/community/:id` detail fetch for durable deep links, Worker `/share/:id` Open Graph preview pages with canonical links, image alt metadata, and nonce-based Worker security headers, `/community?fighter=:id`, short shared-cache headers for public feed/detail/share/media traffic, no-store missing lookups, opaque revocable public media routes, account name/email/Clerk photo/internal owner id/private photo hash redaction, neutral `Player` attribution until a separate handle opt-in exists, Worker-side full-launch-animation checks, and actionable publish errors. | Production Action `32743113442` passed publication, opaque media, owner-only originals/RAW, cross-account clone merge, Unpublish revocation, and cleanup with two dedicated Google users |
| Community abuse controls | `0013_community_moderation.sql`, `worker/src/fighters.ts`, `worker/src/moderation.ts`, `worker/src/clerkWebhooks.ts`, `src/services/CommunityModeration.ts`, `CommunityPage`, and admin-only `ModerationPage` implement signed-in structured reports with 500-character bounded details, one deduplicated/reopenable row per reporter/fighter, a 10/day user limit, owner self-report rejection, admin-gated queue reads, mandatory audit notes for closing decisions, and deliberate unpublish actions. Signed Clerk user webhooks grant or revoke moderation access exclusively from the operator's private `insert_player_role=admin` metadata; public/unsafe metadata cannot authorize an admin, and revocation does not overwrite a future non-admin paid plan. Report counts never trigger automatic removal. Closed records expire after one year; report/user foreign keys are privacy-safe on account deletion. Real Miniflare D1 tests cover deduplication, reopening, private/self-report rejection, role synchronization/enforcement, audit notes, and manual unpublish. | Production operator metadata is pinned and enforced by the smoke-user workflow; the real report/review browser flow remains pending |
| Launch/share metadata | `index.html`, `public/site.webmanifest`, and `public/assets/app-*` / `public/assets/social-card.*` provide app description/theme metadata, installable app icons, and a 1200x630 social card; `scripts/check-production.mjs` and `scripts/smoke-frontend-live.mjs` guard those assets. Public metadata and generated PNG assets use `Insert Player` / `P1`, with target domain `https://insertplayer.ai`. | Implemented and deployed to the full production app |
| Frontend edge headers | `public/_headers` is a self-only fallback. `scripts/frontend-security-headers.mjs` and `configure-frontend-dist.mjs` generate exact per-target policies: production API/FAPI only, sandbox API/exact Development FAPI only, or no external prelaunch origins. `scripts/smoke-frontend-live.mjs` rejects cross-environment origins and unsafe script eval/inline execution after deploy. All modes retain nosniff, referrer policy, frame protection, HSTS, conservative permissions, revalidation/immutable-cache rules, and locally bundled Press Start 2P. | Implemented, tested, and deployed in QA and production |
| Public asset privacy | Community serialization hides original/raw URLs, account/Clerk owner ids, owner-scoped R2 paths, and private photo hashes. `/assets/*` validates versioned `users/` keys and requires the authenticated namespace owner for active and archived `source_versions`/`sprite_versions`. `/public-assets/fighters/*` resolves only current clean source/sprite assets through opaque fighter/revision routes, re-checks publication, uses a one-minute browser/five-minute edge revalidation window, and becomes `404` with `no-store` after Unpublish. Community clone copies only clean public sources/sprites from fighters with the full launch animation set; same-photo owned fighters merge missing public sprites instead of returning an unplayable shell. | Deployed QA payload, source, sprite, share page, legacy URL rejection, and revocation checks passed |
| Production runbook | `PRODUCTION_READINESS.md` covers Cloudflare, Clerk, Stripe, env, migrations, and smoke tests. | Implemented locally |
| Live config helper | `scripts/apply-live-config.mjs` reads ignored env files/process env, validates live Clerk/Stripe/front-end origin shapes and cleared-brand config before mutation, verifies the Stripe key/account/fixed catalog, rejects denylisted accounts and the internal Pages domain for final live config, runs `npm run check:production`, patches production Worker vars, writes `.env.production`, uploads Worker secrets without printing values, keeps Wrangler diagnostic logs in ignored `.wrangler-logs/`, and can redeploy the Worker through `npm run config:live`; `scripts/wrangler-workspace-log.mjs` gives package-level Wrangler deploy/migration commands the same workspace log path; `scripts/check-frontend-live-env.mjs` prevents Pages deploys without live Clerk/frontend env and matching `.brand-clearance.json`; `scripts/deploy-frontend-pages.mjs` requires a cleared-brand `ASF_PAGES_PROJECT_NAME`, runs production/frontend-live/build checks, deploys `dist/`, and runs Pages smoke after upload. | Implemented; Clerk/Stripe values are staged in GitHub production and the Google-only beta is ready for owner promotion |
| Frontend Pages deploy smoke | The build intentionally has no top-level `404.html`, enabling Pages native SPA rendering without a legacy catch-all rewrite. The live/sandbox smoke verifies direct routes, Checkout return URLs, exact Worker/Clerk bundle config, target-specific CSP/security headers, metadata, and old-test absence. The separate prelaunch build remains a credential-free checked emergency artifact. | Full production app and QA app are deployed and remotely smoked |
| Production verifier | `scripts/check-production.mjs` runs frontend guard/typecheck, 328 Vitest tests across 63 files, provider benchmarks, Worker typecheck, isolated prelaunch build/scan, migration replay through `0024`, durable-job concurrency/reconnect and per-artifact resume guards, Clerk lifecycle, real-D1 auth/admin tests, exact CSP tests, upload idempotency, progressive cloud hydration, provider-session/accounting controls, fee-aware pricing, Stripe reconciliation, pseudonymization, moderation, retention, exact Arcade-source restoration/restart guards, match-preserving fighter deletion, safe asset deletion, bounded multipart provider caching, proxy/client hardening, and community/privacy checks. | Worker `0.18.0` passed from a clean worktree and both GitHub production gates on 2026-08-24 |
| Live readiness verifier | `scripts/check-live-readiness.mjs` checks production-shaped Wrangler bindings including Workflow/Container, the full Pages app and Checkout return routes, frontend live env, cleared-brand URLs, Clerk JWKS, Wrangler auth, required Worker secrets including `GENERATION_JOB_SIGNING_SECRET`, pinned Stripe vars, remote D1 schema through `0024` after promotion, R2 lifecycle, and mandatory `/health`. Calls have bounded timeouts and logs stay in ignored `.wrangler-logs/`. | Passed again in production Action `32767504225` against `https://insertplayer.ai`, Worker `0.18.0`, D1, R2, Clerk, and live Stripe bindings |
| Live Worker smoke verifier | `scripts/smoke-live.mjs` reads ignored production env files/process env, checks deployed health readiness flags including Turnstile, provider budget, and pseudonymized anonymous identities, allowed-origin CORS, provider-session browser preflight, tiers, billing packs, signed-out policy including report/moderation routes, public privacy/profile shapes, malformed routes, proxy hardening, provider-session enforcement including GET polling and temp uploads, optional `429` behavior, and share-page security headers. Anonymous Rookie without a challenge must return `403 turnstile_required` and no provider session. With two Clerk JWTs, launch smoke additionally covers authenticated D1/R2 fighter persistence, idempotent uploads, publish/clone privacy, unused reservation release, Stripe reconciliation, ownership guards, and match stats. | Public live smoke and authenticated production Action `32743113442` pass against Worker `0.18.0` version `e1d68b66-31fd-4d2b-b912-d835fc503e6a`; both dedicated Google sessions and all synthetic test assets were cleaned up |
| Final launch gate | `scripts/check-launch.mjs` and `npm run check:launch` require HTTPS Worker/Pages URLs, fresh JWTs from two different Clerk users, the owner brand record, and recent manual browser/provider/two-device evidence, then run production checks, live readiness, Pages smoke, and hard authenticated Worker smoke with bounded timeouts. Manual schema v5 records the launch charge boundary: untouched reservations release before provider use, while upstream failures retain committed credits and cost evidence. | Turnstile replay, two-user production auth, durable Cloudflare CI, and the disposable Development Clerk smoke are complete; remaining evidence is the QA-approved Arcade roster, one live purchase, support-mail proof, real-phone/two-device play, and Production key rotation |
| Match/stat persistence | `FightScene` dispatches `MATCH_COMPLETE_EVENT`; `App.tsx` calls `reportMatchCompletion`; `worker/src/index.ts` records unranked matches through `/api/matches`, requires both reported cloud fighter ids to belong to the signed-in Clerk user before writing stats, constrains opponent/winner ids to signed-in user plus CPU/local system users, bounds rounds/duration/id fields, updates basic signed-in win/loss records for unranked launch matches without changing Elo, and `/api/stats` returns recent matches for the signed-in user. Parameterized stats reads are authenticated and own-user-only. HomePage surfaces the signed-in record, recent results, and public fight board through `src/services/Leaderboard.ts`; public leaderboard rows use rank ids instead of exposing Clerk user ids. | Production Action `32743113442` passed authenticated own-user stats reads; a real completed match on physical devices still needs to prove the write path end to end |
| Build gate | `npm run build:sandbox` passed under Node `22.23.2`; Gallery, Roster, Create, Community, and Moderation are route-lazy, reducing the initial JS from `524.21 kB` / `156.47 kB` gzip to about `343 kB` / `102 kB` gzip. Phaser remains a separate `355.62 kB` gzip fight chunk. `scripts/check-production.mjs` rejects static reintroduction of the heavy routes. | Passed locally and deployed on 2026-08-22 |
| Production check gate | `npm run check:production` passes 328 tests across 63 files plus provider benchmarks: frontend/Worker types, exact live/sandbox/prelaunch CSP isolation, credential-free prelaunch scan, real Miniflare D1/R2 concurrency, durable identical-job-start replay, pre-provider release contract, first-provider charge commit/failed-spend retention, exact three-source/four-animation checkpoint resume, concurrent release race cleanup, hash-manifest sync/promotion, resumable hydration, Clerk/auth/admin behavior, billing/moderation/privacy, provider accounting without global caps, profitability, image processing, exact Arcade source/hash/restart guards, match-preserving fighter deletion, safe asset deletion, bounded multipart proxy streams, retention, tier parity, migration replay through `0024`, and hardening scans. | Passed from a clean worktree and GitHub Actions on 2026-08-24 |
| Route smoke | Vite and Pages served `/menu`, `/fighters/new`, `/gallery`, `/community`, `/roster/cpu`, `/roster/vs`, `/roster/watch`, `/privacy`, `/terms`, `/refunds`, and `/moderation` as direct SPA routes. A signed-in deployed-browser pass loaded the lazy Menu/Gallery/Create/Community/Roster/Moderation surfaces without application errors and entered `/fight` with a `1024x576` Phaser canvas. | Passed locally and on sandbox Pages |
| Cloudflare auth/deploy | Production main `b7c96cf` and develop `1d3ea1e` have the same production-safe tree `6564088a`. Both run Worker `0.18.0`, isolated D1/R2 through `0024`, separate Workflow/Container resources, their own secrets, and canonical Pages branches. Production Action `32767504225` and development Action `32767773857` passed complete checks, migrations, Worker/Container/Workflow deploys, API smokes, Pages deploys, and readiness with the durable account-owned token. | Production and QA are healthy; Cloudflare audit logs confirm the CI actor is an account API token |

## Missing Before Complete

- Prove external delivery/reply identity for the already-routed `privacy@insertplayer.ai` and `support@insertplayer.ai` aliases.
- Completed 2026-08-24: the Clerk Development Backend API key is installed in the GitHub `development` environment and disposable sandbox Action `32768251105` passed with two users, D1/R2 writes, clone/privacy checks, cleanup, webhooks, and tombstones. Production Google OAuth is public and Action `32743113442` already passed with two dedicated users; create a replacement Production Backend API key for GitHub, verify it, then delete the key previously exposed in chat. Apple is post-beta and must remain disabled until dedicated credentials and an end-to-end production sign-in are verified.
- Completed 2026-08-24: the durable Cloudflare account token is installed in both GitHub environments and production/development Actions `32767504225` / `32767773857` passed end to end.
- Finish the 13-fighter official Arcade roster through the guarded Gemini-only production workflow. Keep every fighter private until its real source, three canonical views, 11 animations, alpha cleanup, anatomy, identity, and match play pass QA; activation is always a separate explicit step.
- Repeat the already-validated sandbox Checkout path once in live mode, verifying terms, tax, reusable Customer binding, and idempotent webhook credit grants.
- Completed 2026-08-24: a real production Turnstile token authorized one anonymous Rookie provider session; exact replay failed `403` without a second session or inference.
- Run the remaining browser smoke from `PRODUCTION_READINESS.md`, including real-phone touch QA, a second-device sign-in/import/play test, and production payment/auth checks; carry the existing all-tier provider evidence into `.launch-validation.json`, then run `npm run check:launch` with `ASF_WORKER_URL`, `ASF_FRONTEND_URL`, `ASF_CLERK_JWT`, and `ASF_CLERK_JWT_CLONE`.

Conclusion: not complete yet. The full production app, isolated Cloudflare stacks, DNS/certificates, owner-reviewed brand, Clerk Production with dedicated Google OAuth, Clerk Development CI smoke, Live Stripe configuration, validated sandbox purchases, all three real tiers, durable artifact-level resume, private-by-default content boundary, version-preserving sync, live Turnstile replay proof, repeated authenticated smoke, durable Cloudflare CI, and 328-test suite are in place. The objective still requires the QA-approved official Arcade roster, one live purchase, support-mail proof, real-phone QA, and physical two-device validation. Apple is explicitly deferred and disabled for beta rather than shipped in a broken state.

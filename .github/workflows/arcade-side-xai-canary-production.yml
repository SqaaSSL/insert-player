name: Run production XAI SIDE realism canary

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: Enter ARCADE_SIDE_XAI_TRUMP_REALISTIC_V1 to authorize exactly one paid SIDE call
        required: true
        type: string
      resume_run_id:
        description: Optional prior canary run ID whose immutable checkpoint should be resumed
        required: false
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: production-worker-mutations
  cancel-in-progress: false

jobs:
  canary:
    name: Donald Trump by pinned XAI provider
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment: production
    env:
      REQUESTED_CONFIRMATION: ${{ inputs.confirmation }}
      RESUME_RUN_ID: ${{ inputs.resume_run_id }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      PIXCLI_API_KEY: ${{ secrets.PIXCLI_API_KEY }}
      PIXCLI_BASE_URL: https://pixcli.hilo.cx

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Validate the sealed one-call request
        shell: bash
        run: |
          [[ "$REQUESTED_CONFIRMATION" == "ARCADE_SIDE_XAI_TRUMP_REALISTIC_V1" ]] || {
            printf '::error::The exact XAI SIDE canary confirmation phrase is required.\n'
            exit 1
          }
          [[ -n "$PIXCLI_API_KEY" ]] || {
            printf '::error::PIXCLI_API_KEY is unavailable in the production environment.\n'
            exit 1
          }
          [[ -n "$CLOUDFLARE_API_TOKEN" ]] || {
            printf '::error::CLOUDFLARE_API_TOKEN is unavailable in the production environment.\n'
            exit 1
          }
          if [[ -n "$RESUME_RUN_ID" && ! "$RESUME_RUN_ID" =~ ^[0-9]+$ ]]; then
            printf '::error::resume_run_id must be a numeric GitHub Actions run ID.\n'
            exit 1
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install frontend dependencies
        run: npm ci

      - name: Prove provider prompt isolation and one-call runner
        run: npm run test:arcade:xai-canary

      - name: Restore a prior paid checkpoint
        if: inputs.resume_run_id != ''
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh run download "$RESUME_RUN_ID"
          --repo "${{ github.repository }}"
          --name arcade-side-xai-trump-v1-state
          --dir .

      - name: Restore and verify the private licensed Trump source
        run: npm run arcade:sources -- --target=production --slug=donald-trump

      - name: Execute exactly one pinned XAI SIDE request
        run: >-
          npm run arcade:canary:xai-side --
          --execute
          --confirm=ARCADE_SIDE_XAI_TRUMP_REALISTIC_V1
          --state=.arcade-side-xai-canary-state.json
          --output-dir=.artifacts/arcade-side-xai-canary

      - name: Publish experiment summary
        if: always()
        shell: bash
        run: |
          summary=.artifacts/arcade-side-xai-canary/arcade-side-xai-trump-realistic-adult-v1/summary.md
          if [[ -f "$summary" ]]; then
            cat "$summary" >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload immutable audit bundle and resumable state
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: arcade-side-xai-trump-v1-state
          path: |
            .arcade-side-xai-canary-state.json
            .artifacts/arcade-side-xai-canary/
          if-no-files-found: warn
          retention-days: 90
          include-hidden-files: true
